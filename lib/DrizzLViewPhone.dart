import 'package:flutter/material.dart';

class DrizzLViewPhone extends StatefulWidget {
  const DrizzLViewPhone({Key? key}) : super(key: key);

  @override
  State<DrizzLViewPhone> createState() => _DrizzLViewPhoneState();
}

class _DrizzLViewPhoneState extends State<DrizzLViewPhone> {
  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;
    return Container();
  }
}
